# K7Notes Mobile Environment Variables

# API URL for the web app (browser runs on the same machine as the API)
EXPO_PUBLIC_WEB_API_URL=http://localhost:4000

# API URL for native apps (real devices need a tunnel like ngrok to reach the API)
EXPO_PUBLIC_NATIVE_API_URL=http://localhost:4000
